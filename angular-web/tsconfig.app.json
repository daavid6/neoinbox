/* To learn more about Typescript configuration file: https://www.typescriptlang.org/docs/handbook/tsconfig-json.html. */
/* To learn more about Angular compiler options: https://angular.dev/reference/configs/angular-compiler-options. */
{
	"extends": "./tsconfig.json",
	"compilerOptions": {
		"outDir": "./out-tsc/app",
		// estas ya están en el @types, así que veo redundante esto.
		"types": ["gapi", "google.accounts", "google.picker"]
	},
	"files": ["src/main.ts"],
	// supuestamente ya deberia mirar en los @types, pero algo pasa que no mira.
	"include": ["src/**/*.d.ts", "node_modules/@types/**/*.d.ts"]
}
